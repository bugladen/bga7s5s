> Part of **create-city-event-card**. Open from [SKILL.md](SKILL.md) only when the shape table routes here - keep WHYs intact; do not summarize away regression traps.

## When You Finish

1. Re-read the card text and walk through each clause — confirm each maps to exactly one branch you wrote. **Pure** Forced (no choice) → card class only (`bas/_04cd07`, `bas/_04cd19`). **Forced "must choose"** → State + JS still required (`bas/_04cd11`) — do not invent a City Action/Reaction. Continuous unlabeled location rules (e.g. cannot refuse) are **not** Forced — wire Daichi-style, not `handleEvent` on a challenge event.
2. For every new state class, verify all three are present: the class file, the `States.php` constant, and the entry in the correct `states.inc.php` EVENTS map (`HIGH_DRAMA_PLAYER_TURN_EVENTS` for High Drama; `DUSK_PHASE_BEGIN_EVENTS` for beginning-of-Dusk pickers). Multi-step keys reached via `createTransitionEvent` after an EVENTS hop (e.g. `"04cd09_3"`) must be registered even if no Action ever transitions there by name from `nextState` alone.
3. For every new state, verify JS wiring in the expansion files (`OnEnteringState.faf.js` / `OnEnteringState.bas.js`, matching `OnUpdateActionButtons` / `OnLeavingState`). Add to `PlayerActions.js` action map if reusing client actions. Hand-discard states also need an `EventHandlers.js` branch to enable `actChooseDiscardCards`. Forced "must" pickers: Confirm only — **no** Pass; zombie auto-picks a legal target.
4. Mentally run the pre-commit hook checks against the files you touched.
5. For reveal Forced: confirm `EventCityCardAddedToLocation` + `cardId == $this->Id` (not `cardInCity`). Effects on "this location" at reveal use `$event->location` (e.g. Renown via `createRenownAddedToLocationEvent`). For beginning of Dusk: `EventDuskPhaseBegin` + `cardInCity`. For "does not control this location" effects: do **not** copy Equal Claim's `!$location->isControlled()` early-return unless the effect is specifically "become uncontrolled."
5b. For "character becomes engaged" Forced: `EventCardEngaged` + `!$event->canceled` + `cardInCity` + **`getCharacterById` non-null** (skip attachment engages) + `$character->Location == $this->Location`. Wound via `createCharacterBeingWoundedEvent`, not direct mutate.
6. **Unlimited** City Actions: `$this->setUsed($theah, false)` at resolve; **no** `$playersUsed` / used-list UI. **Once-per-Day** multi-use: `$playersUsed` **and** `setUsed(false)`. Do not conflate the two.
7. If a mid-action step queues engage/discard/move events and then needs another interactive state, `nextState` must go to the phase EVENTS runner with a queued `createTransitionEvent` — do not jump straight to the next custom state or the events sit unprocessed. Same for Forced pickers that queue heals before the next player's turn.
8. Stay-Home during Dusk: new `Game::…` condition per card + `eventCheck` on `EventCardMoved`→Home (not Stubborn cancel on `EventCardMoving`). Do not reuse another card's condition string.
9. Write a journal entry in `.cursor/journal/YYYY-MM-DD-NN-<card>.md` covering the WHY (which existing flag/pattern you reused, what alternatives you considered, anything that looks weird). Read the related faf/bas journals first — they encode hard-won knowledge about edge cases (zombie handling, priority ordering, `CURRENT_PLAYER` vs active player, uncontrolled-location eligibility).
