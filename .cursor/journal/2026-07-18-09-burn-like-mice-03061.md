# Burn like Mice (_03061)

## Classification
1. Pattern A trivial resolve — Forum + Bazaar Renown (no pick)
2. Hero City Action — SchemeCityAction + IAbilityThatTargetsCharacters; Hero trait gate; Home en garde non-Leader → move to performer
3. Forced at end of High Drama — EventHighDramaPhaseEnd + LOCATION_PLAYER_HOME; sequential wound order pick

## WHY HD-end Forced wiring (not Pattern F Planning End)
Card text is High Drama end, not Planning. Precedent for trigger: _03cd12 / EventHighDramaPhaseEnd. Precedent for phase-scoped pick map: HIGH_DRAMA_BEGINNING_01144 under BEGINNING_EVENTS. New: HIGH_DRAMA_END_03061 under HIGH_DRAMA_END_EVENTS (id 6003061).

Same transition string `"03061"` also maps under HIGH_DRAMA_PLAYER_TURN_EVENTS → City Action pick. Separate lookups — Forced uses empty internalId so Card::actFromCardWithId does not route into Action_03061.

## WHY $remainingWoundIds + re-queue transition
Order of wounds matters (reactions between). After each pick: queue wound, if more remain queue another "03061" transition, nextState("") back to END_EVENTS. Wound MEDIUM_PRIORITY before TRANSITION_PRIORITY so reactions fire between picks. Single remaining target auto-wounds on trigger (no order choice). pruneRemainingWoundIds drops anyone who left Home between picks (destroy/move) so UI/zombie don't soft-lock on stale ids.

## WHY Hero not ISorcererAbility
Trait-prefixed City Action keyword — mechanical hasTrait("Hero") only.

## WHY engage=false
Move printed without Engage.

## Uncertain / watch
- Can target own Home characters (literal "a player's Home", no opposing). If Eddie meant opposing-only, add ControllerId gate.
- Text still says "Grande Bazaar"; Game const is Grand — events use const.
- Neutral faction was already on scaffold; left as-is.

## Files
_03061.php, Action_03061.php, State_highDramaEnd_03061.php, State_highDramaPhase03061.php, States.php, states.inc.php, On*.faf.js, references.md, SKILL.md row
