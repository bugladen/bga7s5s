# Astute (Action_03056) — no Renown at target location

## Correction from user

Having Renown is **not** a cost (left of the bullet). Effect text is right of the dot: claim **and** move Renown. So:
- Target is valid even with Renown = 0
- Claim still proceeds
- Skip `03056_2` when no Renown (already handled in `actFromActionWithId`)

## What I changed

Removed Renown gates from `isValidTargetForAbility` and `getValidTargets`. Left the post-claim branch that only queues `03056_2` when `Renown > 0`.

WHY the earlier gate was wrong: I treated "and you move a Renown" as a joint requirement for targeting. Card grammar puts that on the effect side; claim is still useful alone.
