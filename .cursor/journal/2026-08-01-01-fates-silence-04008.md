# Fate's Silence (_04008) — orientation

## Shape
Risk (bas Vodacce). Mirror Fate's Burden `_01025` equip-as-RiskAttachment pattern, NOT Maneuvers (stub had leftover Maneuver imports — already gone).

Printed clauses:
1. **Forced** end-of-HD destroy if equipped → on RiskAttachment (`EventHighDramaPhaseEnd` + `removeRiskAttachment`), same as `_01025_Burden`
2. **Sorcerer Strega Action** equip to **target opposing non-Leader** + cannot be copied → `RiskAction` + `ISorcererAbility` + `IAbilityThatTargetsCharacters` (printed "target") / `IRiskThatTargetsCharacters` on Risk
3. Equipped character **text box blank** — no existing mechanic. Plan: stamp `FATES_SILENCE_CONDITION`, gate ability objects + skip polymorphic Character Forced via Theah core-only dispatch (Harpoon/Shackles condition pattern)

## WHY not Cesca allow-list
Printed "cannot be copied". Burden/Boon are on Cesca's list despite same wording — historical exception, don't expand without Eddie.

## WHY blanking via condition + Theah skip
Ability objects (Action/Reaction/Technique/Maneuver) dispatch from `Card::handleEvent`. Character-class Forced/passives run in subclass `handleEvent` *after* parent — only way to blank those without touching every character is Theah calling `handleCoreCharacterEvent` when blanked. Turais Dall (cited use case) is a Reaction → ability-object gate covers it.

## Stats/Traits
Stub R1/P2/T1/cost0 OK. Fix typo `Sorcerty`→`Sorcery`. Unique already in TraitNames.

## Implementation complete

Wired Fate's Silence as RiskAttachment equip (mirror _01025) + first-class text-box blanking via FATES_SILENCE_CONDITION.

### WHY blanking architecture
Ability objects alone were not enough — Character subclass Forced/passives run after parent::handleEvent. Theah now calls handleCoreCharacterEvent / eventCheckCore when blanked so Turais-style Reactions AND Maryam-style Forced on the Character class both stay silent, while wounds/Harpoon still work.

### WHY not Cesca whitelist
Printed "cannot be copied". Burden/Boon are historical allow-list exceptions despite same wording — do not expand.

### WHY mirror createRiskAttachment sync (Burden)
Proven Risk→FakeAttachment path. Maryam cancel-via-Targeted-batch before sync equip left for later if Eddie wants parity.

### Files
_04008.php, _04008_Silence.php, Action_04008.php, State_highDramaPhase04008.php, Character/Card/Theah/CardAction/Maneuver/Technique gates, Game const, JS trio + Notifications.
