# create-risk skill update from _04010

## Why

Eddie asked to capture lessons from Unravel the Thread (`_04010`) into create-risk so the next deck-reveal Reaction / sink-discard Action does not regress to RiskReaction + early playerReaction.

## What landed

| Pattern | Lesson |
|---|---|
| **D.5** | Gamble-reveal of **this** card → `CardReaction` not `RiskReaction`; `createTransitionEvent` (pri 8) under `DUEL_GAMBLE_REVEALED_EVENTS` **after** chooseList; Use/Pass both return to events; re-reveal new ids only; Parry via Game global; `addCardToWorld($this)` before act for setUsed |
| **B.4** | Sink up to N from one discard: always pile chooser incl. empty City Discard; Pass=0; City→City Deck; then draw + sink self |

## Files touched

`SKILL.md`, `pattern-b.md`, `pattern-d.md`, `wiring.md`, `checklist.md` (§54–55), `references.md`, `helpers.md`

## Not in skill (still open on card)

`_04010.php` WealthCost still `0` vs printed 2 — skill does not claim that was fixed.
