# Confusion (_03068) — create-risk

## Orientation
Eddie asked finish `_03068` via create-risk. Text is a single City Reaction. Recent nearby work: Ambitious `_03067`, Shackles/Lodestone attachments.

## Text
`<b>City Reaction:</b> After an opponent passes • They must move an en garde character from their <b>Home</b> to a <b>City</b> location.`

## Classification
- Pattern D.1 — multi-stage cross-player RiskReaction (pay then opponent chooses)
- Trigger: `EventHighDramaPhasePlayerPassed` (from `actHighDramaPass`) — first card to listen to this event
- City Reaction gate: `getCharactersInCityByPlayerId(owner)` non-empty (mirror `Reaction_02053`)
- Availability also requires passer has ≥1 en garde at Home — "They must move" is moot otherwise; hide the offer
- No printed "Target" → no `IRiskThatTargetsCharacters`
- WealthCost 0 — still goes through pay state (same as Manipulative `_03010`)

## WHY reaction buttons, not GameStates
Opponent must pick character then City destination. Existing reactions already use button lists for both (`Reaction_03010` muster pick, `Reaction_01039` / `Reaction_03040` location pick). Avoids new States.php constants, GameState classes, and On*.faf.js trio. Mirror Manipulative stage machine: `''` → pay → `EventRiskReactionTriggered` → `chooseCharacter` → `chooseLocation`.

## WHY public stage fields
Multi-stage + active player flips to opponent across serialize/DB. Same WHY as `Reaction_03044` public `$stage`.

## Pass timing notes
- Non-final pass: `nextState("pass")` → `HIGH_DRAMA_PLAYER_TURN_EVENTS` (has reaction/pay) — normal.
- Final pass: `nextState("end")` → `HIGH_DRAMA_END` → `HIGH_DRAMA_END_EVENTS` (also has reaction/pay). Queued `PlayerPassed` still processes there before Plunder. Not Studio-tested for Confusion specifically — watch this path.

## Line endings
Write on `_03068.php` produced doubled CR (`0D 0D 0A`) again — fixed to CRLF. Reaction file landed clean CRLF. Don't "normalize to LF".

## Skill update (same day)
Eddie asked to fold Confusion learnings into create-risk. Shipped:
- SKILL.md: City Reaction row + D.1.1 shape-table row + combo/_03068 + finish traps
- pattern-d.md: City Reaction intro; D.1 public-fields + buttons-over-GameStates; new D.1.1; `EventHighDramaPhasePlayerPassed` semantics
- references.md: `_03068` row
- checklist.md: item 1/3/6 updates + item 45
- helpers.md: City Reaction / Home en garde helper notes

## Not done
- Not Studio playtested
- Final-pass reaction path especially worth a manual check
