# create-risk skill update — La Voix (_03034) learnings

## Why

Eddie asked to capture session learnings from implementing `_03034` into create-risk. Prior skill had A.1/A.2, engage-cost notes, and en garde verb item 16, but no worked Diplomat City Action that composes engage-at-announcement + same-location friendly En Garde + may-heal/else-draw. Future agents would otherwise invent Pass-button UX or defer engage until target confirm.

## What was added

1. **Canonical ref** `_03034` La Voix des Sans Voix
2. **Ability-shape table** — Diplomat listed with other trait prefixes; Pattern A.3 row for engage + en garde friendly + may heal/draw
3. **Engage timing note** under the engage-cost section — pay on `EventActionTriggered` when a chooser follows; defer only when engage+effect are one confirm
4. **Pattern A.3** full subsection — performer filters, Engaged targets, auto-draw when `Wounds==0`, `{id:1}`/`{id:2}` labeled buttons not Pass, two GameState + JS trio
5. **Reference table** row + combining-examples sentence
6. **When You Finish** item 25; item 16 refs expanded; item 1 mentions A.3
7. **Fixed** Cross-Cutting `canChallenge()` blurb (was wrongly claiming it checks Engaged)

## Feelings

A.3 is composition more than invention — the valuable bits are engage-before-chooser timing and "don't use Pass when the alternate is Draw." Auto-draw on zero wounds mirrors 01049 and is easy to miss.
