# Action_01143 CanBeClaimed Availability Gate

Eddie: Action_01143 needs to check `theah->canLocationBeClaimedBy` in order to be used.

## Decision Before Edit

Same claim-gate sweep as 01141 / 01030 / 01075 today. Resolve path already guarded claim
emit with `canLocationBeClaimedBy` (EventLocationPressureResult). Availability only
required an unengaged Influence-pressure character — so the scheme City Action could still
be offered when every such character stood at an unclaimable location.

WHY fix availability/performer selection, not only resolve: purpose is pressure-then-claim
(you win ties). Offering it when claim is impossible wastes engage + turn. Filtering
performers matters because RequiresPerformerSelected — one blocked location must not hide
the action if another eligible performer is at a claimable location.

WHY `canLocationBeClaimedBy` not `$location->CanBeClaimed`: central API, `$playerId`
reserved for future per-player claim rules.

## End State

Private `getEligiblePerformers()` shared by `isAvailableToPlayer` and
`getPerformersForAction`. Requires: not Engaged, `canPressure(INFLUENCE)`, and
`canLocationBeClaimedBy($playerId, location)`.

Kept resolve-time recheck. WHY: claimability can change between availability and pressure
result.

Validation:
- `php -l modules/php/cards/_7s5s/actions/Action_01143.php`: clean
- `ReadLints` on Action_01143.php: clean
- Output also in `_results/2026-07-17-01143-canbeclaimed.md`
