# Worldly `_04060`

## Context
BAS Neutral Risk after Silver Tongue `_04059`. WealthCost 0, Riposte 2 / Dashed Parry / Thrust 0. Traits Discovery + Flourish (already in TraitNames).

## Classification
`<b>City Action:</b> Choose a controlled City location with no Renown or no characters • It becomes uncontrolled.`
→ **Pattern A.16** (new). Location unclaim chooser. Mirror Status Matters `Action_01086` filters/emit, but as `RiskCityAction` (City gate, not plain Action) and filter is Renown==0 **or** no characters (not Mercenary-only).

`<b>Maneuver:</b> +1[Thrust]. If this location is uncontrolled, +1[Riposte] instead.`
→ Pure-calc exclusive "instead" branch. **Not** C.10 (always Riposte + resolve claim/unclaim). Actor location via `getControllerForLocation == 0`.

## WHY no performer pick / no Cesca
Text says "Choose a … location" — never "your performer" or character Target. City Action only needs `RiskCityAction`'s city-character presence gate. JS mirrors `01086` (location only), not `04039` (performer highlight).

## WHY any controller, not yours-only
Printed "a controlled City location" — same as `_01086` / C.10 unclaim. A.12's yours-only filter is because lose-control is a *cost you pay* before claim.

## WHY exclusive Thrust/Riposte
"Instead" means one stat, not both. Tempting to copy C.10's always-Riposte + If, but that would give Riposte when controlled too (wrong) or Thrust+Riposte when uncontrolled (wrong).

## Done
- `_04060.php` — IHasActions + IHasManeuvers
- `Action_04060` — RiskCityAction location chooser → unclaim
- `Maneuver_04060` — pure-calc Thrust or Riposte-if-uncontrolled
- `State_highDramaPhase04060` + States const + states.inc.php `"04060"`
- bas JS enter/buttons/leave trio (01086 shape)
- Skill: Pattern A.16 + pure-calc instead note + references/checklist
- php -l clean
