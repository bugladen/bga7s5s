# Action_01206 CanBeClaimed Availability Gate

Eddie: Action_01206 needs to check `theah->canLocationBeClaimedBy` in order to be used.

## Decision Before Edit

Same claim-gate sweep as 01143 / 01141 / 01075 today. Captain's Coat (City Attachment Action).
Resolve path already guarded claim emit with `canLocationBeClaimedBy` (EventLocationPressureResult).
Availability only required city owner, Influence pressure, and coat not Engaged — so the action
could still be offered when the wearer's location is unclaimable.

WHY fix availability, not only resolve: purpose is pressure-then-claim. Offering it when claim
is impossible wastes engage on the coat + turn. Same shape as Action_01075 (single owner
character, AttachmentAction) — no performer list to filter.

WHY `canLocationBeClaimedBy` not `$location->CanBeClaimed`: central API, `$playerId`
reserved for future per-player claim rules.

## End State

`isAvailableToPlayer` now returns false when
`! canLocationBeClaimedBy($playerId, $owner->Location)`.

Kept resolve-time recheck. WHY: claimability can change between availability and pressure
result.

Validation:
- `php -l modules/php/cards/_7s5s/actions/Action_01206.php`: clean
- `ReadLints` on Action_01206.php: clean
- Output also in `_results/2026-07-17-01206-canbeclaimed.md`
