# Reaction_01008 — copy Maneuver_04058 + Reaction_04058 (Shield Rite)

## Ask
Eddie: Cesca should copy Maneuver_04058 and Reaction_04058.

## Reachability
- **Maneuver:** SorcererAbilityPlayed has performer=actor, no target → only when Cesca is the duel actor who played Shield Rite's Maneuver.
- **Reaction:** has target + performer → fires when Cesca is the protected performer OR the redirected target is at her location (IAbilityThatTargetsCharacters path).

## Approaches (WHY)

### Maneuver — host on Cesca (Katain shape)
Same as `Reaction_02011` for Maneuver_01055/01057: `new Maneuver_04058` + `addManeuver` + `ResolveManeuver` + `DuelCalculateManeuverValues`. Cesca already `IHasManeuvers`. Re-check `isAvailableToPlayer` (adversary Engaged cost + Sorcerer actor) — she pays costs. Teardown via new `$copiedManeuvers` on turn end.

Do **not** set `IsEffectsOnlyCopy` — Cesca pays costs by design (contrast Katain journal 2026-09-23-05).

### Reaction — copyCard + beginCopy (not host on Cesca)
RiskReaction pay path discards the Risk from hand (`FrameworkActionsTrait` after pay). Hosting on Cesca would discard the Character. So: `copyCard("04058")` into hand (wealth 0 + discard-as-played), then `beginCopy($performerId)`.

`beginCopy` seeds:
- `isCescaCopy = true`
- `performerId` from Cesca's new `$sourcePerformerId` (captured from EventSorcererAbilityPlayed)
- synthetic 1-wound `EventCharacterBeingWounded` (original intercept already resolved; Cesca chooses a *new* opposing target)

Decline / invalid-target on Cesca copy must **not** re-release onto performer (would wound ally). Normal Decline still re-releases (02016 fix).

Temp card must not intercept other wounds while waiting — `handleEvent` returns early when `isCescaCopy`.

## Files
- `Reaction_01008.php` — allow-list, sourcePerformerId, copiedManeuvers, both branches
- `Reaction_04058.php` — `isCescaCopy`, `beginCopy`, Decline/invalid/handleEvent gates

## Not done / watch
- Playtest: Maneuver copy mid-duel (+1 Riposte stack + draw if Cesca en garde)
- Playtest: Reaction copy → target chooser → pay 0 → wound opposing; Decline after Copy must not wound performer
- First Risk Reaction on Cesca allow-list — pattern for future Sorcerer RiskReactions

## Bugfix (2026-09-23 later): zero-button soft-lock

Eddie: Copy Reaction_04058 → temp Shield Rite in hand → prompt with **no buttons**.

WHY: `createCardInLocation` does not `addCardToWorld`. `beginCopy` set `performerId` on the orphan createCard instance; `getOwningCard` / playerReaction args loaded a fresh DB card with `performerId=null`. `getReactionButtonProperties` early-returned with no Decline → soft-lock.

Fix:
1. `Reaction_01008`: `addCardToWorld($card)` before `beginCopy`
2. `beginCopy`: addCardToWorld + seed the **world card's** reaction + immediate `updateCardObjectInDb`
3. Buttons: always offer Decline when performer/owner missing (never zero buttons again)
