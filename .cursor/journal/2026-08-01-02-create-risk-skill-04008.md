# create-risk skill update — Fate's Silence (_04008)

## Why

Eddie asked to capture session learnings from implementing `_04008` into create-risk. Prior skill had `_01025` only as a performer-filter mirror in journals / Pattern B prose, never as a first-class "equip RiskAttachment" shape. Blank text box had no pattern at all — future agents would invent per-character ability stripping or put end-of-HD Forced on the Risk class (wrong: Risk is permanently hidden while equipped).

## What was added

1. **Pattern B.2** — Equip via `createRiskAttachment` + FakeAttachment; Forced "if equipped • Destroy" on attachment; "cannot be copied" ≠ Cesca allow-list; printed target still gets Cesca interfaces
2. **Pattern E.3** — `FATES_SILENCE_CONDITION` + Theah core-only Character dispatch; Character-owned ability availability gates; attachment abilities keep working
3. **E.1 clarification** — equipped Forced is not Risk-class Forced
4. **Shape table / finish / checklist / references** — `_01025`, `_04008` rows; combining-examples sentence
5. **Skill description** triggers for equip / blank text box / Fate's Silence

## Feelings

B.2 is mostly Burden rediscovered — the valuable WHYs are (1) Forced ownership on the attachment, (2) Cesca allow-list is opt-in despite "cannot be copied" appearing on allow-listed cards historically, (3) E.3 Theah skip so Character-class Forced blanks without touching every Character file. Don't invent Maneuvers because a stub imported `_04007`-style leftovers.
