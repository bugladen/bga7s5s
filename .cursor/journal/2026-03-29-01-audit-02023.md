# Audit of _02023 (Sir Jack Harding)

Eddie asked me to audit `_02023` against its card text. Read through all files:
- `_02023.php` (card definition)
- `Action_02023.php` (city action logic)
- `Technique_02023.php` (technique logic)
- `State_highDramaPhase02023.php` and `_2.php` (state definitions)
- JS files for both states (OnEntering, OnLeaving, OnUpdateActionButtons)
- `states.inc.php` transition mapping
- `States.php` constant definitions

## Card Text
> **City Action:** Move target opposing non-Leader to an adjacent City location with less Renown.
> **Technique:** If the adversary is a Thug or Mercenary • +1 thrust.

## Verdict: All correct, no fixes needed.

### What I checked
- `cardInCity` guard for City Action availability
- Opposing + non-Leader filtering for target selection
- Adjacent location retrieval with `$includeHome = false` (correct — card says "City location")
- Renown comparison uses `<` against performer's location, which equals the target's location since they're co-located (validated in state 1)
- Server-side validation in both `actFromActionWithId` (state 1) and `actFromActionWithIds` (state 2) covers all edge cases
- Move event created with `$engage = false`
- Technique correctly gates on duel + Thug/Mercenary trait, adds +1 thrust
- States properly registered, transitions correct, JS UI wiring complete

### Notes
- `Reknown` spelling is consistent across the codebase (not a typo per se, just the property name)
- JS state 2 has dead code for `LOCATION_PLAYER_HOME` handling from template — harmless since PHP filters it out with `$includeHome = false`
