# Leverage (_03071) — create-risk

## Orientation
Last: Comforting `_03070` (C.6 excess Parry) + double-CR fix. Eddie: finish `_03071` via Text.

## Text
- This card has -1 cost if your Leader is a Villain or Pirate
- City Action: If this location is controlled by an opponent • Engage an opposing character.

## Classification
- Pattern E cost discount via `getActionFromHandDiscount` on the City Action — mirror `_01159` / `_01160` (Action-only Risks; no Maneuver printed → no combat-card discount channel). WHY not invent a discount-only Maneuver: those exemplars don't; Pattern E Maneuver discounts are for printed Maneuver cards.
- Pattern A City Action + bullet-If as availability filter (A.4/A.6 discipline): location controller is an opposing player.
- Effect: engage opposing character at performer's location. Filter `! Engaged` (engage is a no-op / illegal on already engaged).
- No printed "Target" → no `IRiskThatTargetsCharacters` / `IAbilityThatTargetsCharacters` (heal-another / Confusion / Hop discipline). Private `isValidEngageCharacter` + GameState chooser.
- Trait fix: stub had `Beauracracy` typo; TraitNames only has `Bureaucracy` — use correct spelling.

## Implemented
- `_03071.php` — IHasActions + ActionTrait; Traits Bureaucracy (fixed typo) + Villainous; Action_03071 wired
- `Action_03071` — RiskCityAction; Leader Villain/Pirate `getActionFromHandDiscount`; performer filter = opponent-controlled location + ≥1 en garde opposing; chooser via private `isValidEngageCharacter` (no Target interfaces); engage + ActionResolved; nextState("targetChosen")
- `State_highDramaPhase03071` (403071); `"03071"` under HIGH_DRAMA_PLAYER_TURN_EVENTS
- JS trio in On*.faf.js (03011/03060 shape)

## WHY no combat-card discount Maneuver
Bleed Out / Appealing only discount hand Action. Inventing a discount-only Maneuver would invent a printed Maneuver. Combat play of Leverage stays full WealthCost until Eddie says otherwise.

## Skill fold (create-risk)
- **Pattern A.9** — opponent-controlled location • Engage opposing (`!Engaged`); no Target → private validator + GameState; contrasts `_01159` engarde-self
- **Pattern E.2** — Action-only Leader cost discount via `getActionFromHandDiscount`; do not invent Maneuver for combat-card parity
- Shape table + finish traps + `_03071` composition blurb; references row; checklist #47/#48; pattern-e companion table; Bureaucracy typo note; Cesca skip list includes engage-opposing

## Unfinished
Not Studio playtested.
