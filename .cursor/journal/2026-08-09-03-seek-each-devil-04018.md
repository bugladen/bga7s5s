# Seek Each Devil (_04018) implementation

## Orientation
Prior journal: Drachenblut `_04016`. This session: finish Risk `_04018` via create-risk.

## Text → patterns
1. Academic/Hunter −1 cost → E.2-style `getActionFromHandDiscount` on Action (Action-only; no Maneuver). WHY: combat-card has no Maneuver channel; inventing one would invent a printed ability.
2. En Garde Action Target enemy adjacent City → move performer → other Sorcerer/Monster controllers discard → Pattern B + Cesca Target + multiplayer discard like `_04005_2` filtered.

## WHY choices
- Character chooser not B.1 location chooser: printed **"Target"** an enemy character (Cesca `IAbilityThatTargetsCharacters` / `IRiskThatTargetsCharacters`).
- En Garde = `!$Engaged` precondition, not Engage cost (`_04009`).
- `parent::getPerformersForAction` then filter — home eligible (plain Action, not City Action).
- Move `engage=false` (Regroup / Follow the Thread).
- Sticky `$PlayersToDiscard` on Action + `owner->IsUpdated` — Risk in discard by resolve; buildCity loads discard (`_04005`/`_04009` seat).
- ActionResolved before discard Transition (prio 3 before 8) — HD wraps; discard trailing.
- No Cesca allow-list — not Sorcerer.
- Skip `EventCharacterTargeted` — matches `_04008`/`_03011`/`_01115` bulk; not `_01162`.

## Files
- `_04018.php`, `Action_04018.php`
- `State_highDramaPhase04018` + `_2`
- States.php `404018`/`4040182`, states.inc.php transitions
- bas OnEntering/Update/Leaving + EventHandlers for hand discard enable
- `_results/04018-seek-each-devil.md`

## Weird / watch
- Discard player list computed at target confirm against destination *before* move applies — same as post-move for other players' Sorcerer/Monster presence (performer move doesn't change that set; acting player excluded anyway).
- Empty-hand Sorcerer/Monster controllers omitted at confirm and re-filtered in State_2 onEnteringState.

## Unfinished
No studio smoke test yet. Checklist in `_results`.
