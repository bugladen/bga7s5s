## Elina 01118 audit

Audited `_01118` / `Action_01118` / `Reaction_01118` / `State_highDramaPhase01118` against constructor card text.

**Correction (Eddie):** En garde must *not* apply when Elina is not engaged. Restored `&& $elina->Engaged` alongside the opposed-at-destination check. Original read of card text was wrong for this table's ruling; the `Engaged` gate is intentional.

**WHY the action structure:** Still use `getOpposingCharactersAtLocation` for the "opposed" half (clear + consistent with Theah). Pair with `Engaged` for when en garde actually fires.

**WHY the exception fix:** `actFromCardWithLocations` passes decoded JSON; the chosen location is a string (e.g. `Game::LOCATION_*`), not an object with `Name`. Mirrored `Action_01064`-style sprintf args.

Reaction and SorcererAbilityPlayed wiring left as-is; text matches behavior.

**Feeling:** First pass misread the design intent; `Engaged` is load-bearing, not accidental.
