# Tomoe Sango _04043 (Cleaving Crow)

Finished BAS faction Character: duel adversary −1 Finesse + En Garde end-of-HD claim Reaction.

## Abilities

1. **Passive** — During a duel, Sango's adversary has -1[Finesse].
2. **En Garde Reaction** — At the end of High Drama, if Sango's location is uncontrolled • Claim her location.

## Decisions

**Duel Finesse debuff = Soline lifecycle, Sango-as-participant gate** — Mirror `_01089` Started/End/ChallengerSwapped/DefenderSwapped + `AffectedCharacterId`. WHY not Soline's "your character at her location" gate: Sango's printed text is "Sango's adversary" during a duel — only when she is challenger or defender. Swaps: clear if she leaves; apply if she enters; transfer if her adversary is swapped while she remains. Idempotent `hasCondition` before ±1 (Giacinto, not raw Soline) so double-apply can't stack.

**Condition + client notifs** — `TOMOE_SANGO_CONDITION` in Game.php / seventhseacityoffivesails.js + Notifications handlers. WHY: Soline/Giacinto tooltip pattern; Finesse ModifiedEvent updates the chip but players need the source string on the adversary.

**En Garde Reaction on EventHighDramaPhaseEnd** — Reaction_01045 trigger. En Garde = `!$Engaged` precondition (not Engage cost). Claim recipe = Reaction_03005. WHY both `getControllerForLocation == 0` AND `canLocationBeClaimedBy`: text says uncontrolled; CanBeClaimed alone is wrong for controlled locations and can be false under IW while still uncontrolled. `cardInCity` — Home is never claimable. PerformerId = Sango (she is the claimer presence). No state/JS — button Reaction only.

**Samurai → TraitNames** — novel trait on the stub; checklist 65.

## Files

- `_04043.php`, `Reaction_04043.php`
- `Game.php` condition, `TraitNames.php` Samurai
- `seventhseacityoffivesails.js` + `Notifications.js` condition UI

## Skill update (create-character)

Folded Sango lessons into `.claude/skills/create-character/`:
- SKILL.md shape rows: duel adversary −N[Stat]; En Garde end-of-HD claim uncontrolled
- pattern-a subsection + EventCharacterDestroyed/CardSentToLocker/HighDramaPhaseEnd table rows
- pattern-d "End of High Drama → claim uncontrolled location"
- checklist 98–99, references `_04043` / Soline contrast, helpers (`canLocationBeClaimedBy` + controller `== 0`)

## Mid-duel locker / destroy (final)

Debuff clears **immediately** on Destroy + CardSentToLocker (before EventHub recreate). `stDuelEnd` `clearPendingDebuff` = safety net only. Do **not** defer restore until duel end — user confirmed immediate clear.

