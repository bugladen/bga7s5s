# Raven (_04012) implementation

## Text clauses
1. While Raven has a **Ranged** card in her dueling line, the adversary cannot perform **Maneuvers**.
2. **City Action:** Engage Raven • She issues a [Finesse] challenge to target opposing character. Other characters cannot intervene.

## Pattern choices
- **Passive:** Pattern A `eventCheck` on `EventResolveManeuver` (Maryam `Technique_01186`) gated on `adversaryId == Raven.Id` + duel participant + Ranged in controller's `LOCATION_DUELING_LINE`.
  - WHY Maryam Resolve shape not Maneuver_01129 Activated: adversary-scoped — Raven's own maneuvers stay legal.
  - WHY participant gate (Elena `_03004`): dueling line is per-player; without it another character dueling for Raven's player would falsely credit Raven.
- **City Action:** Pattern F + new `RAVEN_CHALLENGE_TYPE = 27` (Valeri/Torvo no-intervene). Engage printed → trichotomy (a): auto-engage list + `!Engaged`. Finesse via `STAT_FINESSE`. Mirror `Action_03002`/`Action_04002` picker → `"04012_2"` into challenge machine. No `createActionResolvedEvent` in the Action (challenge flow owns it).
- **Args UX:** emptied interveners for Raven AND Valeri/Torvo (they previously relied only on interventionCheck throw). WHY: Pattern F says args filter + server check; empty picker is the no-intervene filter.
- **Reaction_02058:** skip Raven type like Valeri/Torvo — intervene-ban challenges shouldn't prompt Jump In.

## Trait
- Added `Ontoquas` to `TraitNames::$TraitsJson` (checklist item 64). Did **not** invent Eisen trait — stub omitted it.

## State / wiring
- State ID `404012`, name `highDramaPhase04012`
- Transitions: `"04012"` → picker, `"04012_2"` → `HIGH_DRAMA_CHALLENGE_ACTION_TECHNIQUE_AVAILABLE`
- JS OnEntering/Update/Leaving bas — same Confirm+highlight shape as 04002/04011

## Unfinished / watch
- Passive only blocks at `EventResolveManeuver` (Maryam). Adversary may still *select* a Maneuver and fail at resolve — acceptable mirror; Activated-time block would be an enhancement.
- No playtest yet.

## Skill update (same session, follow-up)
Eddie asked to fold learnings into create-character skill.

Added:
- SKILL.md shape rows: adversary-cannot-Maneuver-while-trait-in-line; Other characters cannot intervene
- pattern-a.md: "Adversary cannot Maneuver while trait in dueling line" + `EventResolveManeuver` in phase table; Maryam vs Maneuver_01129 vs Raven comparison table
- pattern-f.md: "Other characters cannot intervene" subsection; Reaction_02058 in integration table; auto-engage list includes DANILO/RAVEN; Action_04012 / Action_01123 in examples
- checklist.md items 74–76; journal list + Raven
- references.md: `_04012` / `Action_04012` / `Technique_01186` / `Action_01123`

WHY call out Maryam Resolve+adversaryId hard: next agent will reach for Maneuver_01129's Activated global ban or invent a sticky flag for a live line condition. WHY call out Reaction_02058 + empty interveners: Valeri/Torvo historically only threw in interventionCheck — Jump In still prompted and the picker still listed characters.
