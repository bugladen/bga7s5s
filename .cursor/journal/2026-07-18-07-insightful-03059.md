# Insightful (_03059)

## Card
Maneuver: Look at top 3 of adversary's deck. Reveal one and add its Parry or Thrust to this card. Replace in any order. If participant is Academic, sink any instead of replacing.

Ussura Risk, Wealth 0, R/P/T dashed/1/1. Traits Virtue / Flourish.

## Classification
Pattern C with C.3 timing (calc-driving choice) + deck peek/reorder UI from Reaction_03052 / Action_02002 / Technique_01010.

WHY not EventResolveManeuver for the look: Parry/Thrust must hit EventDuelCalculateManeuverValues this round — Resolve fires after calc. Mirror Maneuver_03024/03035: EventManeuverActivated + stackEvent.

WHY Academic is not availability gate: sink is optional upgrade ("If … Academic, sink any"). Non-Academic still gets look/reveal/stat/reorder.

WHY immediate sink insert (Technique_01010) not queued createCardAddedToFactionDeckEvent: finishReplaceOrReorder does top inserts in the same act before EVENTS drains — queued sinks would race.

## Flow (03035 stackEvent discipline)
1. Activated: peek 3 into CHOSEN_CARD, stackEvent "03059"
2. Choose 1 to reveal (private chooseList)
3. +Parry / +Thrust buttons from revealed printed stats
4. Academic only: sink any (0+ via Pass) — multi-select
5. Reorder remaining (skip if ≤1)

Each intermediate step stackEvents next transition before nextState("") → EVENTS so pending calc stays behind. Real EventManeuverCanceled clears ChooseParry/BonusAmount/RevealedCardId/AdversaryPlayerId + CHOSEN_CARD.

## Availability
Adversary deck+discard > 0 (getCardsOnTop reshuffles discard; may return <3 — operate on actual N).

## Done
- Maneuver_03059.php, _03059.php (IHasManeuvers only — no IRiskThatTargetsCharacters)
- State_duelResolveManeuver_03059 … _4
- States.php + states.inc.php (RESOLVE + Miyato/Ota TECHNIQUE mirror)
- On*.faf.js + EventHandlers.js
- references.md row; _results/03059-insightful.md
- php -l clean

## Feelings
Heavier than Courageous — four UI states for one Maneuver. The stackEvent discipline from Loyal/Superstitious is the load-bearing part; deck UI was mostly copy-paste from Yevgeni/Elisabetta. If playtest finds Academic sink awkward (Pass vs empty Confirm), that's the place to tune — not the calc timing.

## Fix: reorder + sink exclude revealed pick
Eddie: `_03059_3` and `_03059_4` unchosen cards must not include the card just picked (revealed for Parry/Thrust). WHY: "Replace them" / sink "those" = the other looked-at cards; the reveal pick stays in place. `getUnchosenLookedAtCards()` filters `RevealedCardId` for sink args/act, finishReplaceOrReorder, and _4. Academic step skipped when unchosen count is 0.

## Skill update (post-implement)
Folded as **Pattern C.8** into create-risk:
- `pattern-c.md`: C.8 recipe (C.3 timing + peek/reveal/unchosen sink-reorder; immediate sink vs queued race; Academic not availability; Miyato mirror)
- `SKILL.md`: shape-table row, companion C.1–C.8, combining paragraph, finish trap line
- `checklist.md`: item 1 walk + item 42
- `references.md`: `_03059` row retagged C.8 with unchosen-exclusion WHY

WHY lettered: agents will hit "look at adversary deck, reveal one for combat stats, put the rest back" again; the reveal-pick exclusion and Resolve-vs-Activated timing are the exact regression traps from this session.
