# Comforting (_03070) — create-risk

## Orientation
After Hop on Board `_03069`. Eddie: finish `_03070` via create-risk from Text.

## Text
- Maneuver: Discard threat from your participant in excess of your adversary's stat value used for the duel.
- Example italic: Influence duel → remove threat above adversary's Influence.

## Classification
- Single Pattern C Maneuver — **C.6 variant** (remove via Parry, not move via Riposte)
- Not "remove all" (`Technique_02012` / full-clear) — only `max(0, threat - adversaryDuelStat)`
- Pure calc (`EventDuelCalculateManeuverValues` only); no sub-state / Miyato mirror
- No trait prefix; no printed "Target" → no `IRiskThatTargetsCharacters`
- Traits Flourish + Virtue already in TraitNames

## WHY Parry + CHALLENGE_STAT match
- "Discard/remove" threat = Parry channel (C.6 / Turais). Riposte would *move* threat to adversary — wrong verb.
- Adversary stat = `Game::CHALLENGE_STAT` → Modified Combat/Finesse/Influence — same `match` as Restricted Hostilities in `stDuelEndOfRound`. Do not hardcode Combat; the italic example is Influence.
- Skip explanation when excess == 0; do not hide Maneuver on excess > 0 (same C.6 availability discipline as Wily).
- Printed DashedRiposte irrelevant — effect is Maneuver Parry (and card already has printed Parry 1 as combat-card stats).

## Wiring
- `_03070` implements `IHasManeuvers` + `Maneuver_03070`
- No states / JS / States.php changes

## Not done
- Not Studio playtested

## Skill update (2026-07-21)
Folded C.6 excess into create-risk: `pattern-c.md` (third row + `CHALLENGE_STAT` `match` + WHYs), `SKILL.md` shape table + Finish traps + composition `_03070`, `checklist.md` §1/§37, `references.md` Comforting row, `helpers.md` `getCurrentDuelThreat` / `CHALLENGE_STAT` notes. WHY document now: next agent would otherwise copy Turais remove-all or hardcode Combat and miss the Influence-duel example.
