# _04cd14 Millstone Rhud (bas City Character)

## Card Text (printed)
Negotiable. Forced: After you recruit Millstone • Wound him and an opposing character. Technique: +1 Thrust (×2).

## Note
User said create-city-event-card but class `extends CityCharacter` — used create-city-character instead.

## Classification
1. Negotiable — constructor flag already set
2. Forced interactive — EventCharacterRecruited → wound self always; if ≥1 opposing at his location, transition for mandatory picker to wound them
3. Dual Technique_PlusOneThrust — generic, unique IDs like Langschwert _01048

## Plan / WHY
- Trigger: `EventCharacterRecruited` + `$event->characterId == $this->Id`. Recruit payment already transitions to HIGH_DRAMA_PLAYER_TURN_EVENTS, so queue `"04cd14"` there (same as City Actions).
- Opposing scope: **same location** via `getOpposingCharactersAtLocation`. WHY: wound-opposing without broader language matches Sibella/Adelheide; board-wide would be huge for a Forced. Printed text has no "at this location" but Text says "target" which is the choose keyword, not a range expander.
- No opposing at location: still wound Millstone only; skip transition. Forced can't require an illegal choose.
- Order: queue Millstone wound first, then transition — wound processes before picker UI.
- No Pass; zombie auto-picks first eligible opposing.
- Techniques: Character already has TechniqueTrait — just push two Technique_PlusOneThrust with setId Technique_04cd14_1 / _2 after resetCard (mirror _01048).

## Implemented
- `_04cd14.php` — techniques + handleEvent / args / act / getEligibleOpposingCharacters
- `State_highDramaPhase04cd14.php` — Forced picker, zombie auto-picks first opposing
- `States::HIGH_DRAMA_PLAYER_TURN_04CD14 = 4040014` + `"04cd14"` on HIGH_DRAMA_PLAYER_TURN_EVENTS
- bas JS enter/update/leave for `highDramaPhase04cd14` (highlightCardsAsSelectable + Confirm Character, no Pass)
- Text aligned to printed: "Wound him and an opposing character"
- `_results/2026-07-24-04cd14-millstone.md`

## Status
php -l clean. Not playtested on BGA Studio.

## Gotchas for next agent
- Dual Technique_PlusOneThrust **must** have distinct setId values before setOwnerId, or both collapse to `{ownerId}_Technique_PlusOneThrust`.
- ControllerId is set by EventHub before card handleEvent on EventCharacterRecruited — safe to use for transition active player and opposing filter.
- If Eddie expected create-city-event-card literally: wrong base class on the stub; CityCharacter skill was correct.

## Skill update (same day)
Folded into `create-city-character`:
- Shape table: pure Forced vs interactive Forced ("must choose" / wound him and opposing); bas `_04cdNN` shared-numbering sibling warning
- pattern-b: Interactive Forced recipe (post-recruit Millstone); pure muster wound (Joern); HIGH_DRAMA_PLAYER_TURN_EVENTS registration for recruit Forced
- pattern-e: prefer Technique_PlusOneThrust; dual-copy setId collision WHY
- helpers: EventCharacterRecruited ControllerId timing; opposing = same location default
- wiring: Forced picker no Pass / no PlayerActions.js map
- references + checklist: `_04cd14`, Joern, Langschwert; interactive Forced + dual technique checklist items
- Journal pointer: `2026-07-24-02-04cd14-millstone.md`
