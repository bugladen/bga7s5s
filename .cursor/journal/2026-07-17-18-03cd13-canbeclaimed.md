# Action_03cd13 CanBeClaimed Availability Gate

Eddie: Action_03cd13 needs to check `theah->canLocationBeClaimedBy` in order to be used.

## Decision Before Edit

Same claim-gate sweep as 01075 / 01206 / 03040 today. Resolve path already wrapped
claim emit in `canLocationBeClaimedBy` (else notify "cannot be claimed"). Availability
only checked parents + `playersUsed` + eligible fewer-renown targets — so Crabs in a
Bucket could still be offered when the city event's location was unclaimable.

WHY hard-gate on claimability (unlike 03040's claim-OR-engage): card text payoff is
only "Claim this location" after engage. No alternate branch. Offering when unclaimable
wastes engage + turn for a no-op claim.

WHY `canLocationBeClaimedBy` not `$location->CanBeClaimed`: central API, `$playerId`
reserved for future per-player claim rules.

## End State

`isAvailableToPlayer` after `playersUsed` check:
```
$owner = $this->getOwningCard($theah);
if ( ! $theah->canLocationBeClaimedBy($playerId, $owner->Location)) return false;
```

Kept resolve-time recheck in `handleTargetChosen`. WHY: claimability can change between
availability and target selection.

Validation:
- `php -l modules/php/cards/faf/actions/Action_03cd13.php`: clean
- `ReadLints` on Action_03cd13.php: clean
- Output also in `_results/2026-07-17-03cd13-canbeclaimed.md`
